# Costa OS — AI Layer (optional tiers)
# Tier 0: Cloud-only (no local models, just Claude API)
# (no packages needed, just API key)

# Tier 1: Voice assistant only (Whisper STT, no local LLM)
# whisper.cpp built from source with Vulkan/ROCm
python-numpy

# Tier 2: Voice + Local LLM (Ollama + small model)
ollama
ollama-vulkan  # Vulkan backend — works on all GPUs, avoids ROCm HIP idle bug on RDNA4
python-pytorch
tree-sitter  # AST parsing library (C) — shared by costa-ast daemon
python-dbus  # D-Bus bindings for costa-ast daemon
# python-torchaudio — not in official repos, install via: pip install torchaudio
# python-firecrawl — web scraping SDK, install via: pip install firecrawl-py

# Tier 3: Full AI workstation (large models, training)
# Add ollama-rocm for ROCm support (NVIDIA: ollama-cuda)
# Note: on RDNA4 (gfx1200), use Vulkan not ROCm — HIP pegs GPU at 100% idle

# GPU driver packages (auto-detected)
# AMD: mesa vulkan-radeon lib32-mesa lib32-vulkan-radeon vulkan-icd-loader
# AMD (ROCm optional): rocm-hip-sdk (only if needed for non-Ollama workloads)
# NVIDIA: nvidia nvidia-utils lib32-nvidia-utils cuda
# Intel: mesa vulkan-intel lib32-mesa lib32-vulkan-intel
