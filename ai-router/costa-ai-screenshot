#!/usr/bin/env bash
# Costa AI Screenshot — capture region, analyze with AI, show result
# Wraps screenshot_action.py

SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "$0")")" && pwd)"
exec python3 "$SCRIPT_DIR/screenshot_action.py" "$@"
