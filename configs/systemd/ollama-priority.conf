[Service]
# Give Ollama higher CPU priority so AI stays responsive
# under load (VM, ISO builds, compiles). -10 = elevated but
# not real-time, won't starve games or desktop.
Nice=-10
# Also boost I/O priority for model loading
IOSchedulingClass=best-effort
IOSchedulingPriority=2
