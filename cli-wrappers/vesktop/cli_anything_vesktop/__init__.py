"""CLI-Anything wrapper for Vesktop — deterministic CLI access to Discord state.

Reads Vesktop window titles (via hyprctl) and config files to expose
Discord status, current channel/server, voice state, and settings.

Capabilities: status, channel, voice, settings
"""

__version__ = "0.1.0"
