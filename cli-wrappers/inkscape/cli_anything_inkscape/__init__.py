"""CLI-Anything wrapper for Inkscape — deterministic CLI access to Inkscape editor state.

Uses Inkscape's CLI (--actions) for exports, Hyprland window titles for
runtime state, and XDG recent files for history.

Capabilities: status, open documents, recent files, export
"""

__version__ = "0.1.0"
